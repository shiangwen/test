'''
1. 還記得在大廳地板上看到的半截信封殘篇(fragment.txt)，或許會派上用場，該如何讀取它？
2. 運用留聲機得到的線索，依5個數字的排列分別填入五個seek()的index中解碼，接著在終端機執行 letter3.py 你是否看出端倪了？
3. 如果得到了解答，將這些字視為一個string，快點將解碼謄入手邊的空白密碼紙(password)上！
'''

# 1, 2
# with open("__file name__", "r") as f:
#     f.seek(num)
#     print(f.read(1))
#     f.seek(num)
#     print(f.read(1))
#     f.seek(num)
#     print(f.read(1))
#     f.seek(num)
#     print(f.read(1))
#     f.seek(num)
#     print(f.read(1))

# 3. Record the code into the password.txt.
# with open("__file name__", "_mode_") as g:
#     g.w____('_the code_')